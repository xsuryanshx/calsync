# calsync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Local-first Next.js web app that unifies two Google Calendar accounts into a single read-only week grid, with a manual sync button. Architected so production deploy is a config change, not a rewrite.

**Architecture:** Single Next.js 15 process. Four internal layers (UI → server actions/routes → domain → repository). SQLite now, Postgres later via Drizzle swap. Auth.js v5 handles Google OAuth token collection. Encrypted refresh tokens at rest. `syncUserCalendars()` is a pure orchestrator callable from button today, cron tomorrow.

**Tech Stack:** Next.js 15 (App Router), React 19, TypeScript strict, Drizzle ORM + better-sqlite3, Auth.js v5, googleapis, zod, pino, Vitest, Tailwind v4, better-auth-style token crypto via node `crypto` (AES-256-GCM).

**Spec:** `docs/superpowers/specs/2026-04-10-calsync-design.md`

---

## File Ownership Map (for parallel execution)

Each task owns specific files. Parallel agents MUST NOT touch files outside their task's ownership list.

| Task | Owns (exclusive write access) |
|---|---|
| T1 Scaffold | `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `tailwind.config.ts`, `app/globals.css`, `app/layout.tsx`, `app/page.tsx`, `.env.local.example`, `vitest.config.ts`, `drizzle.config.ts` |
| T2 Config & logger | `lib/config.ts`, `lib/logger.ts`, `tests/config.test.ts` |
| T3 Crypto | `lib/crypto/tokens.ts`, `tests/crypto/tokens.test.ts` |
| T4 DB schema & client | `lib/db/schema.ts`, `lib/db/client.ts`, `drizzle/*` (generated) |
| T5 TokenStore | `lib/db/token-store.ts`, `tests/db/token-store.test.ts` |
| T6 EventStore | `lib/db/event-store.ts`, `tests/db/event-store.test.ts` |
| T7 Google mapper & merge | `lib/google/map.ts`, `lib/google/merge.ts`, `tests/google/map.test.ts`, `tests/google/merge.test.ts` |
| T8 GoogleCalendarClient | `lib/google/client.ts`, `tests/google/client.test.ts` |
| T9 sync orchestrator | `lib/sync/sync-user.ts`, `tests/sync/sync-user.test.ts` |
| T10 Auth.js config | `lib/auth/config.ts`, `app/api/auth/[...nextauth]/route.ts`, `auth.ts` |
| T11 /api routes & server action | `app/api/events/route.ts`, `app/api/sync/route.ts`, `app/actions/sync.ts` |
| T12 Week page + server data | `app/week/page.tsx`, `lib/time/week.ts` |
| T13 Settings page | `app/settings/page.tsx` |
| T14 Week grid components | `components/WeekGrid.tsx`, `components/DayColumn.tsx`, `components/EventBlock.tsx`, `components/AllDayStrip.tsx` |
| T15 SyncButton + interactive pieces | `components/SyncButton.tsx`, `components/EventPopover.tsx`, `components/AccountBadge.tsx`, `components/ReconnectBanner.tsx`, `components/WeekSwitcher.tsx` |
| T16 Seed script | `scripts/seed-test-data.ts` |
| T17 README & env docs | `README.md` |

## Dependency Graph (execution waves)

```
Wave 0: T1 (scaffold)                                        [must complete first]
Wave 1: T2, T3, T4                                           [parallel, depend on T1]
Wave 2: T5, T6, T7                                           [parallel, depend on T4]
Wave 3: T8                                                   [depends on T3, T5]
Wave 4: T9                                                   [depends on T5, T6, T7, T8]
Wave 5: T10, T11                                             [parallel; T10→T5, T11→T9+T6]
Wave 6: T12, T13                                             [parallel; T12→T11+T6, T13→T5]
Wave 7: T14                                                  [depends on T12]
Wave 8: T15                                                  [depends on T11, T14]
Wave 9: T16, T17                                             [parallel, docs/smoke]
```

---

## Task 1: Scaffold Next.js project

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `tailwind.config.ts`, `app/globals.css`, `app/layout.tsx`, `app/page.tsx`, `.env.local.example`, `vitest.config.ts`, `drizzle.config.ts`

- [ ] **Step 1: Initialize Next.js project non-interactively**

Run:
```bash
cd /Users/suryanshrawat/Documents/github/calsync
npx --yes create-next-app@latest . \
  --typescript --tailwind --eslint --app --src-dir=false \
  --import-alias "@/*" --use-npm --no-turbopack --yes
```

Expected: files scaffolded, `package.json`, `app/`, `tsconfig.json` appear. If prompt appears, answer defaults.

- [ ] **Step 2: Install runtime dependencies**

Run:
```bash
npm install drizzle-orm better-sqlite3 googleapis next-auth@beta @auth/drizzle-adapter zod pino pino-pretty
```

- [ ] **Step 3: Install dev dependencies**

Run:
```bash
npm install -D drizzle-kit @types/better-sqlite3 vitest @vitest/ui @testing-library/react @testing-library/jest-dom jsdom nock @types/node tsx
```

- [ ] **Step 4: Add scripts to package.json**

Edit `package.json` `scripts` to add:
```json
{
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "test": "vitest run",
  "test:watch": "vitest",
  "db:generate": "drizzle-kit generate",
  "db:migrate": "drizzle-kit migrate",
  "seed": "tsx scripts/seed-test-data.ts"
}
```

- [ ] **Step 5: Create `.env.local.example`**

Create `.env.local.example`:
```
# Google OAuth (create at console.cloud.google.com)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Auth.js session signing (generate: openssl rand -base64 32)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Refresh-token encryption key (generate: openssl rand -base64 32)
ENCRYPTION_KEY=

# Database
DATABASE_URL=file:./calsync.db
```

- [ ] **Step 6: Create `drizzle.config.ts`**

```ts
import type { Config } from "drizzle-kit";

export default {
  schema: "./lib/db/schema.ts",
  out: "./drizzle",
  dialect: "sqlite",
  dbCredentials: {
    url: process.env.DATABASE_URL?.replace("file:", "") ?? "./calsync.db",
  },
} satisfies Config;
```

- [ ] **Step 7: Create `vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  test: {
    environment: "node",
    globals: true,
    include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
    setupFiles: [],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "."),
    },
  },
});
```

- [ ] **Step 8: Replace `app/page.tsx`**

```tsx
import { redirect } from "next/navigation";
import { listAccountsForUser } from "@/lib/db/token-store";

export default async function Home() {
  const accounts = await listAccountsForUser(1);
  if (accounts.length === 0) {
    redirect("/settings");
  }
  redirect("/week");
}
```

- [ ] **Step 9: Replace `app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "calsync",
  description: "Unified view of your Google Calendars",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="bg-slate-50 text-slate-900 min-h-screen">{children}</body>
    </html>
  );
}
```

- [ ] **Step 10: Verify TypeScript compiles**

Run: `npx tsc --noEmit`
Expected: errors only about missing `lib/db/token-store` (will be created in T5). That's fine for scaffold commit — we'll come back after T5. Skip strict TS error check until T5 lands.

- [ ] **Step 11: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js + install deps

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 2: Config loader & logger

**Files:**
- Create: `lib/config.ts`, `lib/logger.ts`, `tests/config.test.ts`

- [ ] **Step 1: Write failing test for config validation**

Create `tests/config.test.ts`:
```ts
import { describe, it, expect, beforeEach, afterEach } from "vitest";

describe("config", () => {
  const original = { ...process.env };
  beforeEach(() => { process.env = { ...original }; });
  afterEach(() => { process.env = { ...original }; });

  it("throws when required env var is missing", async () => {
    delete process.env.GOOGLE_CLIENT_ID;
    await expect(async () => {
      const mod = await import("@/lib/config?missing=1");
      return mod.config;
    }).rejects.toThrow();
  });

  it("parses valid env", async () => {
    process.env.GOOGLE_CLIENT_ID = "abc";
    process.env.GOOGLE_CLIENT_SECRET = "def";
    process.env.NEXTAUTH_SECRET = "sss";
    process.env.NEXTAUTH_URL = "http://localhost:3000";
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 1).toString("base64");
    process.env.DATABASE_URL = "file:./calsync.db";
    const { loadConfig } = await import("@/lib/config");
    const cfg = loadConfig();
    expect(cfg.googleClientId).toBe("abc");
    expect(cfg.databaseUrl).toBe("file:./calsync.db");
  });
});
```

- [ ] **Step 2: Run test — verify it fails**

Run: `npm test -- config`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `lib/config.ts`**

```ts
import { z } from "zod";

const schema = z.object({
  GOOGLE_CLIENT_ID: z.string().min(1),
  GOOGLE_CLIENT_SECRET: z.string().min(1),
  NEXTAUTH_SECRET: z.string().min(1),
  NEXTAUTH_URL: z.string().url(),
  ENCRYPTION_KEY: z.string().refine(
    (v) => {
      try { return Buffer.from(v, "base64").length === 32; } catch { return false; }
    },
    { message: "ENCRYPTION_KEY must be base64-encoded 32 bytes (openssl rand -base64 32)" }
  ),
  DATABASE_URL: z.string().min(1),
});

export type Config = {
  googleClientId: string;
  googleClientSecret: string;
  nextAuthSecret: string;
  nextAuthUrl: string;
  encryptionKey: Buffer;
  databaseUrl: string;
};

export function loadConfig(): Config {
  const parsed = schema.parse(process.env);
  return {
    googleClientId: parsed.GOOGLE_CLIENT_ID,
    googleClientSecret: parsed.GOOGLE_CLIENT_SECRET,
    nextAuthSecret: parsed.NEXTAUTH_SECRET,
    nextAuthUrl: parsed.NEXTAUTH_URL,
    encryptionKey: Buffer.from(parsed.ENCRYPTION_KEY, "base64"),
    databaseUrl: parsed.DATABASE_URL,
  };
}

let _cached: Config | undefined;
export const config: Config = new Proxy({} as Config, {
  get(_, prop) {
    _cached ??= loadConfig();
    return Reflect.get(_cached, prop);
  },
});
```

- [ ] **Step 4: Implement `lib/logger.ts`**

```ts
import pino from "pino";

export const logger = pino({
  level: process.env.LOG_LEVEL ?? "info",
  transport: process.env.NODE_ENV === "production"
    ? undefined
    : { target: "pino-pretty", options: { colorize: true } },
});
```

- [ ] **Step 5: Run tests**

Run: `npm test -- config`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add lib/config.ts lib/logger.ts tests/config.test.ts
git commit -m "feat: config loader + logger

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 3: Token crypto (AES-256-GCM)

**Files:**
- Create: `lib/crypto/tokens.ts`, `tests/crypto/tokens.test.ts`

- [ ] **Step 1: Write failing round-trip test**

Create `tests/crypto/tokens.test.ts`:
```ts
import { describe, it, expect, beforeEach } from "vitest";

describe("tokenCrypto", () => {
  beforeEach(() => {
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 7).toString("base64");
  });

  it("round-trips a refresh token", async () => {
    const { encryptToken, decryptToken } = await import("@/lib/crypto/tokens");
    const plain = "1//0gRefreshTokenSampleAbC123";
    const encrypted = encryptToken(plain);
    expect(encrypted).not.toBe(plain);
    expect(encrypted.split(":")).toHaveLength(3);
    const back = decryptToken(encrypted);
    expect(back).toBe(plain);
  });

  it("throws on tampered ciphertext", async () => {
    const { encryptToken, decryptToken } = await import("@/lib/crypto/tokens");
    const encrypted = encryptToken("secret");
    const [iv, tag, ct] = encrypted.split(":");
    const tampered = [iv, tag, ct.slice(0, -2) + "00"].join(":");
    expect(() => decryptToken(tampered)).toThrow();
  });
});
```

- [ ] **Step 2: Run test — verify failure**

Run: `npm test -- tokens`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `lib/crypto/tokens.ts`**

```ts
import { createCipheriv, createDecipheriv, randomBytes } from "node:crypto";

const ALGO = "aes-256-gcm";
const IV_LENGTH = 12;

function getKey(): Buffer {
  const raw = process.env.ENCRYPTION_KEY;
  if (!raw) throw new Error("ENCRYPTION_KEY is not set");
  const buf = Buffer.from(raw, "base64");
  if (buf.length !== 32) {
    throw new Error("ENCRYPTION_KEY must decode to 32 bytes");
  }
  return buf;
}

export function encryptToken(plaintext: string): string {
  const iv = randomBytes(IV_LENGTH);
  const cipher = createCipheriv(ALGO, getKey(), iv);
  const ct = Buffer.concat([cipher.update(plaintext, "utf8"), cipher.final()]);
  const tag = cipher.getAuthTag();
  return `${iv.toString("base64")}:${tag.toString("base64")}:${ct.toString("base64")}`;
}

export function decryptToken(payload: string): string {
  const [ivB64, tagB64, ctB64] = payload.split(":");
  if (!ivB64 || !tagB64 || !ctB64) throw new Error("malformed ciphertext");
  const decipher = createDecipheriv(ALGO, getKey(), Buffer.from(ivB64, "base64"));
  decipher.setAuthTag(Buffer.from(tagB64, "base64"));
  const pt = Buffer.concat([
    decipher.update(Buffer.from(ctB64, "base64")),
    decipher.final(),
  ]);
  return pt.toString("utf8");
}
```

- [ ] **Step 4: Run tests**

Run: `npm test -- tokens`
Expected: PASS (both tests)

- [ ] **Step 5: Commit**

```bash
git add lib/crypto/tokens.ts tests/crypto/tokens.test.ts
git commit -m "feat: AES-256-GCM token crypto

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 4: DB schema & client

**Files:**
- Create: `lib/db/schema.ts`, `lib/db/client.ts`, `drizzle/*` (generated)

- [ ] **Step 1: Create `lib/db/schema.ts`**

```ts
import { sqliteTable, integer, text, uniqueIndex } from "drizzle-orm/sqlite-core";

export const users = sqliteTable("users", {
  id: integer("id").primaryKey({ autoIncrement: true }),
  email: text("email"),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
});

export const accounts = sqliteTable(
  "accounts",
  {
    id: integer("id").primaryKey({ autoIncrement: true }),
    userId: integer("user_id").notNull().references(() => users.id),
    googleEmail: text("google_email").notNull(),
    encryptedRefreshToken: text("encrypted_refresh_token").notNull(),
    accessToken: text("access_token"),
    accessTokenExpiresAt: integer("access_token_expires_at", { mode: "timestamp" }),
    displayColor: text("display_color").notNull(),
    createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
    updatedAt: integer("updated_at", { mode: "timestamp" }).notNull(),
  },
  (t) => ({
    userEmailUnique: uniqueIndex("accounts_user_email_uniq").on(t.userId, t.googleEmail),
  })
);

export const events = sqliteTable(
  "events",
  {
    id: integer("id").primaryKey({ autoIncrement: true }),
    userId: integer("user_id").notNull().references(() => users.id),
    accountId: integer("account_id").notNull().references(() => accounts.id),
    googleEventId: text("google_event_id").notNull(),
    icalUid: text("ical_uid"),
    title: text("title"),
    description: text("description"),
    location: text("location"),
    startTs: integer("start_ts", { mode: "timestamp" }).notNull(),
    endTs: integer("end_ts", { mode: "timestamp" }).notNull(),
    isAllDay: integer("is_all_day", { mode: "boolean" }).notNull().default(false),
    tz: text("tz"),
    status: text("status"),
    responseStatus: text("response_status"),
    htmlLink: text("html_link"),
    hangoutLink: text("hangout_link"),
    rawJson: text("raw_json"),
    syncedAt: integer("synced_at", { mode: "timestamp" }).notNull(),
  },
  (t) => ({
    accountEventUnique: uniqueIndex("events_account_event_uniq").on(
      t.accountId,
      t.googleEventId
    ),
  })
);

export const syncState = sqliteTable("sync_state", {
  accountId: integer("account_id").primaryKey().references(() => accounts.id),
  lastSyncToken: text("last_sync_token"),
  lastFullSyncAt: integer("last_full_sync_at", { mode: "timestamp" }),
});

export type User = typeof users.$inferSelect;
export type Account = typeof accounts.$inferSelect;
export type NewAccount = typeof accounts.$inferInsert;
export type Event = typeof events.$inferSelect;
export type NewEvent = typeof events.$inferInsert;
```

- [ ] **Step 2: Create `lib/db/client.ts`**

```ts
import Database from "better-sqlite3";
import { drizzle, BetterSQLite3Database } from "drizzle-orm/better-sqlite3";
import { migrate } from "drizzle-orm/better-sqlite3/migrator";
import { sql } from "drizzle-orm";
import { config } from "@/lib/config";
import * as schema from "./schema";
import { users } from "./schema";

type Db = BetterSQLite3Database<typeof schema>;

let _db: Db | undefined;
let _migrated = false;

export function getDb(urlOverride?: string): Db {
  if (_db) return _db;
  const url = urlOverride ?? config.databaseUrl;
  const file = url.startsWith("file:") ? url.slice(5) : url;
  const sqlite = new Database(file);
  sqlite.pragma("journal_mode = WAL");
  sqlite.pragma("foreign_keys = ON");
  _db = drizzle(sqlite, { schema });
  if (!_migrated) {
    migrate(_db, { migrationsFolder: "./drizzle" });
    const existing = _db.select().from(users).where(sql`id = 1`).all();
    if (existing.length === 0) {
      _db.insert(users).values({ id: 1, email: null, createdAt: new Date() }).run();
    }
    _migrated = true;
  }
  return _db;
}

// Test helper: reset singleton
export function __resetDbForTests() {
  _db = undefined;
  _migrated = false;
}
```

- [ ] **Step 3: Generate initial migration**

Run: `npm run db:generate`
Expected: `drizzle/0000_*.sql` file created

- [ ] **Step 4: Verify migration applies**

Run: `DATABASE_URL=file:./test-boot.db npx tsx -e "require('./lib/db/client').getDb('file:./test-boot.db')"`
Expected: no errors, `test-boot.db` file created

Run: `rm test-boot.db`

- [ ] **Step 5: Commit**

```bash
git add lib/db/schema.ts lib/db/client.ts drizzle/
git commit -m "feat: drizzle schema + sqlite client with boot migration

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 5: TokenStore

**Files:**
- Create: `lib/db/token-store.ts`, `tests/db/token-store.test.ts`

- [ ] **Step 1: Write failing tests**

Create `tests/db/token-store.test.ts`:
```ts
import { describe, it, expect, beforeEach } from "vitest";
import fs from "node:fs";
import path from "node:path";

const TEST_DB = path.join(__dirname, "test-token-store.db");

describe("TokenStore", () => {
  beforeEach(() => {
    if (fs.existsSync(TEST_DB)) fs.unlinkSync(TEST_DB);
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 9).toString("base64");
    process.env.DATABASE_URL = `file:${TEST_DB}`;
    process.env.GOOGLE_CLIENT_ID = "x";
    process.env.GOOGLE_CLIENT_SECRET = "x";
    process.env.NEXTAUTH_SECRET = "x";
    process.env.NEXTAUTH_URL = "http://localhost:3000";
  });

  it("upserts an account and retrieves decrypted refresh token", async () => {
    const { __resetDbForTests } = await import("@/lib/db/client");
    __resetDbForTests();
    const store = await import("@/lib/db/token-store");
    const saved = await store.upsertAccount({
      userId: 1,
      googleEmail: "a@example.com",
      refreshToken: "raw-refresh-1",
      accessToken: "raw-access-1",
      accessTokenExpiresAt: new Date(Date.now() + 3600_000),
      displayColor: "#3b82f6",
    });
    expect(saved.id).toBeGreaterThan(0);
    const accounts = await store.listAccountsForUser(1);
    expect(accounts).toHaveLength(1);
    expect(accounts[0].googleEmail).toBe("a@example.com");
    const refresh = await store.getDecryptedRefreshToken(saved.id);
    expect(refresh).toBe("raw-refresh-1");
  });

  it("update path overwrites on re-upsert", async () => {
    const { __resetDbForTests } = await import("@/lib/db/client");
    __resetDbForTests();
    const store = await import("@/lib/db/token-store");
    await store.upsertAccount({
      userId: 1, googleEmail: "a@example.com", refreshToken: "v1",
      accessToken: null, accessTokenExpiresAt: null, displayColor: "#3b82f6",
    });
    await store.upsertAccount({
      userId: 1, googleEmail: "a@example.com", refreshToken: "v2",
      accessToken: "new-access", accessTokenExpiresAt: new Date(), displayColor: "#3b82f6",
    });
    const accounts = await store.listAccountsForUser(1);
    expect(accounts).toHaveLength(1);
    const refresh = await store.getDecryptedRefreshToken(accounts[0].id);
    expect(refresh).toBe("v2");
  });

  it("assigns blue to first account, green to second (if color not passed)", async () => {
    const { __resetDbForTests } = await import("@/lib/db/client");
    __resetDbForTests();
    const store = await import("@/lib/db/token-store");
    const a = await store.upsertAccount({
      userId: 1, googleEmail: "a@example.com", refreshToken: "r",
      accessToken: null, accessTokenExpiresAt: null,
    });
    const b = await store.upsertAccount({
      userId: 1, googleEmail: "b@example.com", refreshToken: "r",
      accessToken: null, accessTokenExpiresAt: null,
    });
    expect(a.displayColor).toBe("#3b82f6");
    expect(b.displayColor).toBe("#10b981");
  });
});
```

- [ ] **Step 2: Run — verify failure**

Run: `npm test -- token-store`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `lib/db/token-store.ts`**

```ts
import { eq, and, sql } from "drizzle-orm";
import { getDb } from "./client";
import { accounts, Account } from "./schema";
import { encryptToken, decryptToken } from "@/lib/crypto/tokens";

export const DEFAULT_COLORS = ["#3b82f6", "#10b981"] as const;

export type UpsertAccountInput = {
  userId: number;
  googleEmail: string;
  refreshToken: string;
  accessToken: string | null;
  accessTokenExpiresAt: Date | null;
  displayColor?: string;
};

export async function upsertAccount(input: UpsertAccountInput): Promise<Account> {
  const db = getDb();
  const encryptedRefreshToken = encryptToken(input.refreshToken);
  const now = new Date();

  const existing = db.select().from(accounts)
    .where(and(eq(accounts.userId, input.userId), eq(accounts.googleEmail, input.googleEmail)))
    .all();

  if (existing.length > 0) {
    db.update(accounts)
      .set({
        encryptedRefreshToken,
        accessToken: input.accessToken,
        accessTokenExpiresAt: input.accessTokenExpiresAt,
        updatedAt: now,
      })
      .where(eq(accounts.id, existing[0].id))
      .run();
    return db.select().from(accounts).where(eq(accounts.id, existing[0].id)).all()[0];
  }

  let color = input.displayColor;
  if (!color) {
    const countRow = db.select({ c: sql<number>`count(*)` }).from(accounts)
      .where(eq(accounts.userId, input.userId)).all();
    const count = Number(countRow[0]?.c ?? 0);
    color = DEFAULT_COLORS[count % DEFAULT_COLORS.length];
  }

  const inserted = db.insert(accounts).values({
    userId: input.userId,
    googleEmail: input.googleEmail,
    encryptedRefreshToken,
    accessToken: input.accessToken,
    accessTokenExpiresAt: input.accessTokenExpiresAt,
    displayColor: color,
    createdAt: now,
    updatedAt: now,
  }).returning().all();

  return inserted[0];
}

export async function listAccountsForUser(userId: number): Promise<Account[]> {
  const db = getDb();
  return db.select().from(accounts).where(eq(accounts.userId, userId)).all();
}

export async function getAccountById(id: number): Promise<Account | null> {
  const db = getDb();
  const rows = db.select().from(accounts).where(eq(accounts.id, id)).all();
  return rows[0] ?? null;
}

export async function getDecryptedRefreshToken(accountId: number): Promise<string> {
  const row = await getAccountById(accountId);
  if (!row) throw new Error(`account ${accountId} not found`);
  return decryptToken(row.encryptedRefreshToken);
}

export async function updateAccessToken(
  accountId: number,
  accessToken: string,
  expiresAt: Date
): Promise<void> {
  const db = getDb();
  db.update(accounts)
    .set({ accessToken, accessTokenExpiresAt: expiresAt, updatedAt: new Date() })
    .where(eq(accounts.id, accountId))
    .run();
}
```

- [ ] **Step 4: Run tests**

Run: `npm test -- token-store`
Expected: PASS (all 3 tests)

- [ ] **Step 5: Commit**

```bash
git add lib/db/token-store.ts tests/db/token-store.test.ts
git commit -m "feat: TokenStore with encrypted refresh tokens

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 6: EventStore

**Files:**
- Create: `lib/db/event-store.ts`, `tests/db/event-store.test.ts`

- [ ] **Step 1: Write failing tests**

Create `tests/db/event-store.test.ts`:
```ts
import { describe, it, expect, beforeEach } from "vitest";
import fs from "node:fs";
import path from "node:path";

const TEST_DB = path.join(__dirname, "test-event-store.db");

async function setup() {
  if (fs.existsSync(TEST_DB)) fs.unlinkSync(TEST_DB);
  process.env.ENCRYPTION_KEY = Buffer.alloc(32, 9).toString("base64");
  process.env.DATABASE_URL = `file:${TEST_DB}`;
  process.env.GOOGLE_CLIENT_ID = "x";
  process.env.GOOGLE_CLIENT_SECRET = "x";
  process.env.NEXTAUTH_SECRET = "x";
  process.env.NEXTAUTH_URL = "http://localhost:3000";
  const { __resetDbForTests } = await import("@/lib/db/client");
  __resetDbForTests();
  const token = await import("@/lib/db/token-store");
  const a = await token.upsertAccount({
    userId: 1, googleEmail: "a@example.com", refreshToken: "r",
    accessToken: null, accessTokenExpiresAt: null,
  });
  return a.id;
}

describe("EventStore.replaceWindow", () => {
  beforeEach(() => {});

  it("inserts fresh events, overwrites existing, and drops cancelled events inside the window", async () => {
    const accountId = await setup();
    const store = await import("@/lib/db/event-store");

    const window = {
      start: new Date("2026-04-06T00:00:00Z"),
      end: new Date("2026-04-20T00:00:00Z"),
    };

    // Initial pull
    await store.replaceWindow(accountId, 1, window, [
      {
        googleEventId: "e1",
        icalUid: null,
        title: "Meeting 1",
        description: null, location: null,
        startTs: new Date("2026-04-08T10:00:00Z"),
        endTs: new Date("2026-04-08T11:00:00Z"),
        isAllDay: false, tz: "UTC",
        status: "confirmed", responseStatus: "accepted",
        htmlLink: null, hangoutLink: null, rawJson: "{}",
      },
      {
        googleEventId: "e2",
        icalUid: null,
        title: "Meeting 2",
        description: null, location: null,
        startTs: new Date("2026-04-10T14:00:00Z"),
        endTs: new Date("2026-04-10T15:00:00Z"),
        isAllDay: false, tz: "UTC",
        status: "confirmed", responseStatus: "accepted",
        htmlLink: null, hangoutLink: null, rawJson: "{}",
      },
    ]);

    let fetched = await store.listEventsInWindow(1, window);
    expect(fetched).toHaveLength(2);

    // Second pull — e1 updated, e2 removed (cancelled), e3 added
    await store.replaceWindow(accountId, 1, window, [
      {
        googleEventId: "e1",
        icalUid: null,
        title: "Meeting 1 updated",
        description: null, location: null,
        startTs: new Date("2026-04-08T10:00:00Z"),
        endTs: new Date("2026-04-08T11:00:00Z"),
        isAllDay: false, tz: "UTC",
        status: "confirmed", responseStatus: "accepted",
        htmlLink: null, hangoutLink: null, rawJson: "{}",
      },
      {
        googleEventId: "e3",
        icalUid: null,
        title: "Meeting 3",
        description: null, location: null,
        startTs: new Date("2026-04-12T09:00:00Z"),
        endTs: new Date("2026-04-12T10:00:00Z"),
        isAllDay: false, tz: "UTC",
        status: "confirmed", responseStatus: "accepted",
        htmlLink: null, hangoutLink: null, rawJson: "{}",
      },
    ]);

    fetched = await store.listEventsInWindow(1, window);
    const titles = fetched.map((e) => e.title).sort();
    expect(titles).toEqual(["Meeting 1 updated", "Meeting 3"]);
  });

  it("filters declined events at read time", async () => {
    const accountId = await setup();
    const store = await import("@/lib/db/event-store");
    const window = {
      start: new Date("2026-04-06T00:00:00Z"),
      end: new Date("2026-04-20T00:00:00Z"),
    };
    await store.replaceWindow(accountId, 1, window, [
      {
        googleEventId: "e1", icalUid: null, title: "Yes",
        description: null, location: null,
        startTs: new Date("2026-04-08T10:00:00Z"),
        endTs: new Date("2026-04-08T11:00:00Z"),
        isAllDay: false, tz: "UTC",
        status: "confirmed", responseStatus: "accepted",
        htmlLink: null, hangoutLink: null, rawJson: "{}",
      },
      {
        googleEventId: "e2", icalUid: null, title: "No",
        description: null, location: null,
        startTs: new Date("2026-04-08T12:00:00Z"),
        endTs: new Date("2026-04-08T13:00:00Z"),
        isAllDay: false, tz: "UTC",
        status: "confirmed", responseStatus: "declined",
        htmlLink: null, hangoutLink: null, rawJson: "{}",
      },
    ]);
    const fetched = await store.listEventsInWindow(1, window);
    expect(fetched.map((e) => e.title)).toEqual(["Yes"]);
  });
});
```

- [ ] **Step 2: Run — verify failure**

Run: `npm test -- event-store`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `lib/db/event-store.ts`**

```ts
import { and, eq, gte, lt, ne, asc } from "drizzle-orm";
import { getDb } from "./client";
import { events, Event } from "./schema";

export type EventInput = {
  googleEventId: string;
  icalUid: string | null;
  title: string | null;
  description: string | null;
  location: string | null;
  startTs: Date;
  endTs: Date;
  isAllDay: boolean;
  tz: string | null;
  status: string | null;
  responseStatus: string | null;
  htmlLink: string | null;
  hangoutLink: string | null;
  rawJson: string;
};

export type TimeWindow = { start: Date; end: Date };

/**
 * Delete all events for this account whose start is inside [window.start, window.end),
 * then insert the fresh set. Transactional.
 */
export async function replaceWindow(
  accountId: number,
  userId: number,
  window: TimeWindow,
  fresh: EventInput[]
): Promise<void> {
  const db = getDb();
  db.transaction((tx) => {
    tx.delete(events)
      .where(
        and(
          eq(events.accountId, accountId),
          gte(events.startTs, window.start),
          lt(events.startTs, window.end)
        )
      )
      .run();
    if (fresh.length === 0) return;
    const now = new Date();
    for (const e of fresh) {
      tx.insert(events).values({
        userId,
        accountId,
        googleEventId: e.googleEventId,
        icalUid: e.icalUid,
        title: e.title,
        description: e.description,
        location: e.location,
        startTs: e.startTs,
        endTs: e.endTs,
        isAllDay: e.isAllDay,
        tz: e.tz,
        status: e.status,
        responseStatus: e.responseStatus,
        htmlLink: e.htmlLink,
        hangoutLink: e.hangoutLink,
        rawJson: e.rawJson,
        syncedAt: now,
      }).run();
    }
  });
}

/**
 * List non-declined events for a user in the window, sorted by start time.
 */
export async function listEventsInWindow(
  userId: number,
  window: TimeWindow
): Promise<Event[]> {
  const db = getDb();
  return db.select().from(events)
    .where(
      and(
        eq(events.userId, userId),
        gte(events.startTs, window.start),
        lt(events.startTs, window.end),
        ne(events.responseStatus, "declined")
      )
    )
    .orderBy(asc(events.startTs))
    .all();
}
```

- [ ] **Step 4: Run tests**

Run: `npm test -- event-store`
Expected: PASS (both tests)

- [ ] **Step 5: Commit**

```bash
git add lib/db/event-store.ts tests/db/event-store.test.ts
git commit -m "feat: EventStore with replaceWindow semantics and declined filter

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 7: Google event mapper & merge helpers

**Files:**
- Create: `lib/google/map.ts`, `lib/google/merge.ts`, `tests/google/map.test.ts`, `tests/google/merge.test.ts`

- [ ] **Step 1: Write failing tests for `map`**

Create `tests/google/map.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { mapGoogleEvent } from "@/lib/google/map";
import type { calendar_v3 } from "googleapis";

describe("mapGoogleEvent", () => {
  it("maps a timed event", () => {
    const g: calendar_v3.Schema$Event = {
      id: "abc",
      iCalUID: "abc@google.com",
      summary: "Standup",
      description: "daily",
      location: "Zoom",
      status: "confirmed",
      start: { dateTime: "2026-04-10T09:00:00-07:00", timeZone: "America/Los_Angeles" },
      end:   { dateTime: "2026-04-10T09:30:00-07:00", timeZone: "America/Los_Angeles" },
      htmlLink: "https://calendar.google.com/x",
      hangoutLink: "https://meet.google.com/xyz",
      attendees: [{ self: true, responseStatus: "accepted" }],
    };
    const m = mapGoogleEvent(g);
    expect(m.googleEventId).toBe("abc");
    expect(m.title).toBe("Standup");
    expect(m.isAllDay).toBe(false);
    expect(m.startTs.toISOString()).toBe("2026-04-10T16:00:00.000Z");
    expect(m.endTs.toISOString()).toBe("2026-04-10T16:30:00.000Z");
    expect(m.responseStatus).toBe("accepted");
    expect(m.tz).toBe("America/Los_Angeles");
  });

  it("maps an all-day event", () => {
    const g: calendar_v3.Schema$Event = {
      id: "ad1",
      iCalUID: "ad1@google.com",
      summary: "Holiday",
      status: "confirmed",
      start: { date: "2026-04-12" },
      end:   { date: "2026-04-13" },
    };
    const m = mapGoogleEvent(g);
    expect(m.isAllDay).toBe(true);
    expect(m.startTs.toISOString()).toBe("2026-04-12T00:00:00.000Z");
    expect(m.endTs.toISOString()).toBe("2026-04-13T00:00:00.000Z");
    expect(m.responseStatus).toBe("accepted"); // no attendees → treat as organizer
  });

  it("derives responseStatus from self attendee", () => {
    const g: calendar_v3.Schema$Event = {
      id: "d1", summary: "Decl", status: "confirmed",
      start: { dateTime: "2026-04-10T09:00:00Z" },
      end:   { dateTime: "2026-04-10T10:00:00Z" },
      attendees: [
        { email: "other@example.com", responseStatus: "accepted" },
        { self: true, responseStatus: "declined" },
      ],
    };
    expect(mapGoogleEvent(g).responseStatus).toBe("declined");
  });
});
```

- [ ] **Step 2: Write failing tests for `merge`**

Create `tests/google/merge.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { filterAndSort } from "@/lib/google/merge";
import type { Event } from "@/lib/db/schema";

function mk(overrides: Partial<Event>): Event {
  return {
    id: 0, userId: 1, accountId: 1,
    googleEventId: "g", icalUid: null,
    title: "t", description: null, location: null,
    startTs: new Date("2026-04-10T10:00:00Z"),
    endTs: new Date("2026-04-10T11:00:00Z"),
    isAllDay: false, tz: "UTC",
    status: "confirmed", responseStatus: "accepted",
    htmlLink: null, hangoutLink: null, rawJson: "{}",
    syncedAt: new Date(),
    ...overrides,
  };
}

describe("filterAndSort", () => {
  it("drops declined events and sorts by start time", () => {
    const evs = [
      mk({ id: 1, title: "B", startTs: new Date("2026-04-10T11:00:00Z") }),
      mk({ id: 2, title: "decl", responseStatus: "declined" }),
      mk({ id: 3, title: "A", startTs: new Date("2026-04-10T09:00:00Z") }),
    ];
    const out = filterAndSort(evs);
    expect(out.map((e) => e.title)).toEqual(["A", "B"]);
  });
});
```

- [ ] **Step 3: Run — verify failure**

Run: `npm test -- google`
Expected: FAIL (modules not found)

- [ ] **Step 4: Implement `lib/google/map.ts`**

```ts
import type { calendar_v3 } from "googleapis";
import type { EventInput } from "@/lib/db/event-store";

export function mapGoogleEvent(g: calendar_v3.Schema$Event): EventInput {
  const isAllDay = Boolean(g.start?.date && !g.start?.dateTime);
  const startIso = g.start?.dateTime ?? g.start?.date;
  const endIso = g.end?.dateTime ?? g.end?.date;
  if (!startIso || !endIso) {
    throw new Error(`event ${g.id} has no start/end`);
  }
  const startTs = new Date(startIso);
  const endTs = new Date(endIso);

  const selfAttendee = (g.attendees ?? []).find((a) => a.self);
  const responseStatus = selfAttendee?.responseStatus ?? "accepted";

  return {
    googleEventId: g.id ?? "",
    icalUid: g.iCalUID ?? null,
    title: g.summary ?? null,
    description: g.description ?? null,
    location: g.location ?? null,
    startTs,
    endTs,
    isAllDay,
    tz: g.start?.timeZone ?? null,
    status: g.status ?? null,
    responseStatus,
    htmlLink: g.htmlLink ?? null,
    hangoutLink: g.hangoutLink ?? null,
    rawJson: JSON.stringify(g),
  };
}
```

- [ ] **Step 5: Implement `lib/google/merge.ts`**

```ts
import type { Event } from "@/lib/db/schema";

export function filterAndSort(events: Event[]): Event[] {
  return events
    .filter((e) => e.responseStatus !== "declined")
    .sort((a, b) => a.startTs.getTime() - b.startTs.getTime());
}
```

- [ ] **Step 6: Run tests**

Run: `npm test -- google`
Expected: PASS (all 4 tests)

- [ ] **Step 7: Commit**

```bash
git add lib/google/map.ts lib/google/merge.ts tests/google/
git commit -m "feat: google event mapper + merge helpers

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 8: GoogleCalendarClient (with refresh handling)

**Files:**
- Create: `lib/google/client.ts`, `tests/google/client.test.ts`

- [ ] **Step 1: Write failing tests**

Create `tests/google/client.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

describe("GoogleCalendarClient", () => {
  beforeEach(() => {
    process.env.GOOGLE_CLIENT_ID = "cid";
    process.env.GOOGLE_CLIENT_SECRET = "csec";
    process.env.NEXTAUTH_SECRET = "s";
    process.env.NEXTAUTH_URL = "http://localhost:3000";
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 1).toString("base64");
    process.env.DATABASE_URL = "file:./test-gcal.db";
    vi.resetModules();
  });

  it("throws ReauthRequired on invalid_grant", async () => {
    vi.doMock("googleapis", () => {
      return {
        google: {
          auth: {
            OAuth2: class {
              setCredentials() {}
              async getAccessToken() {
                const err: any = new Error("invalid_grant");
                err.response = { data: { error: "invalid_grant" } };
                throw err;
              }
            },
          },
          calendar: () => ({ events: { list: vi.fn() } }),
        },
      };
    });
    const { GoogleCalendarClient, ReauthRequired } = await import("@/lib/google/client");
    const client = new GoogleCalendarClient({
      accountId: 1, refreshToken: "r", accessToken: null, accessTokenExpiresAt: null,
    });
    await expect(client.listEvents({
      timeMin: new Date(), timeMax: new Date(),
    })).rejects.toBeInstanceOf(ReauthRequired);
  });

  it("lists events successfully", async () => {
    const mockList = vi.fn().mockResolvedValue({
      data: {
        items: [
          {
            id: "e1", summary: "hi", status: "confirmed",
            start: { dateTime: "2026-04-10T09:00:00Z" },
            end: { dateTime: "2026-04-10T10:00:00Z" },
          },
        ],
      },
    });
    vi.doMock("googleapis", () => ({
      google: {
        auth: {
          OAuth2: class {
            setCredentials() {}
            async getAccessToken() { return { token: "at" }; }
          },
        },
        calendar: () => ({ events: { list: mockList } }),
      },
    }));
    const { GoogleCalendarClient } = await import("@/lib/google/client");
    const client = new GoogleCalendarClient({
      accountId: 1, refreshToken: "r",
      accessToken: "at-cached", accessTokenExpiresAt: new Date(Date.now() + 3600_000),
    });
    const out = await client.listEvents({
      timeMin: new Date("2026-04-06"), timeMax: new Date("2026-04-20"),
    });
    expect(out).toHaveLength(1);
    expect(out[0].id).toBe("e1");
    expect(mockList).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run — verify failure**

Run: `npm test -- client`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `lib/google/client.ts`**

```ts
import { google, calendar_v3 } from "googleapis";
import { config } from "@/lib/config";
import { logger } from "@/lib/logger";

export class ReauthRequired extends Error {
  constructor(public accountId: number, message = "reauth required") {
    super(message);
    this.name = "ReauthRequired";
  }
}

export class SyncFailed extends Error {
  constructor(public reason: string) {
    super(reason);
    this.name = "SyncFailed";
  }
}

export type ClientCredentials = {
  accountId: number;
  refreshToken: string;
  accessToken: string | null;
  accessTokenExpiresAt: Date | null;
};

export type ListEventsArgs = {
  timeMin: Date;
  timeMax: Date;
  calendarId?: string;
};

export class GoogleCalendarClient {
  constructor(private creds: ClientCredentials) {}

  private buildAuth() {
    const oauth2 = new google.auth.OAuth2(
      config.googleClientId,
      config.googleClientSecret
    );
    oauth2.setCredentials({
      refresh_token: this.creds.refreshToken,
      access_token: this.creds.accessToken ?? undefined,
      expiry_date: this.creds.accessTokenExpiresAt?.getTime(),
    });
    return oauth2;
  }

  async listEvents(args: ListEventsArgs): Promise<calendar_v3.Schema$Event[]> {
    const auth = this.buildAuth();

    // Force token refresh if expired or missing
    try {
      await auth.getAccessToken();
    } catch (err: any) {
      if (err?.response?.data?.error === "invalid_grant" || /invalid_grant/.test(err?.message ?? "")) {
        throw new ReauthRequired(this.creds.accountId);
      }
      throw new SyncFailed(`token refresh failed: ${err?.message ?? err}`);
    }

    const calendar = google.calendar({ version: "v3", auth });

    const attempt = async (n: number): Promise<calendar_v3.Schema$Event[]> => {
      try {
        const res = await calendar.events.list({
          calendarId: args.calendarId ?? "primary",
          timeMin: args.timeMin.toISOString(),
          timeMax: args.timeMax.toISOString(),
          singleEvents: true,
          showDeleted: false,
          maxResults: 2500,
          orderBy: "startTime",
        });
        return res.data.items ?? [];
      } catch (err: any) {
        const code = err?.code ?? err?.response?.status;
        if ((code === 429 || (code >= 500 && code < 600)) && n < 3) {
          const delay = 1000 * 2 ** n;
          logger.warn({ accountId: this.creds.accountId, code, n, delay }, "google api retry");
          await new Promise((r) => setTimeout(r, delay));
          return attempt(n + 1);
        }
        if (err?.response?.data?.error === "invalid_grant") {
          throw new ReauthRequired(this.creds.accountId);
        }
        throw new SyncFailed(`events.list failed: ${err?.message ?? err}`);
      }
    };

    return attempt(0);
  }
}
```

- [ ] **Step 4: Run tests**

Run: `npm test -- client`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/google/client.ts tests/google/client.test.ts
git commit -m "feat: GoogleCalendarClient with refresh + retry + reauth detection

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 9: sync orchestrator

**Files:**
- Create: `lib/sync/sync-user.ts`, `tests/sync/sync-user.test.ts`

- [ ] **Step 1: Write failing integration test**

Create `tests/sync/sync-user.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import fs from "node:fs";
import path from "node:path";

const TEST_DB = path.join(__dirname, "test-sync.db");

describe("syncUserCalendars", () => {
  beforeEach(() => {
    if (fs.existsSync(TEST_DB)) fs.unlinkSync(TEST_DB);
    process.env.GOOGLE_CLIENT_ID = "cid";
    process.env.GOOGLE_CLIENT_SECRET = "csec";
    process.env.NEXTAUTH_SECRET = "s";
    process.env.NEXTAUTH_URL = "http://localhost:3000";
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 3).toString("base64");
    process.env.DATABASE_URL = `file:${TEST_DB}`;
    vi.resetModules();
  });

  it("syncs two accounts; writes events; reports per-account status", async () => {
    const mockList = vi.fn()
      .mockResolvedValueOnce({ data: { items: [
        { id: "a1", summary: "one", status: "confirmed",
          start: { dateTime: "2026-04-10T09:00:00Z" },
          end: { dateTime: "2026-04-10T10:00:00Z" } },
      ] } })
      .mockResolvedValueOnce({ data: { items: [
        { id: "b1", summary: "two", status: "confirmed",
          start: { dateTime: "2026-04-11T09:00:00Z" },
          end: { dateTime: "2026-04-11T10:00:00Z" } },
      ] } });

    vi.doMock("googleapis", () => ({
      google: {
        auth: {
          OAuth2: class {
            setCredentials() {}
            async getAccessToken() { return { token: "at" }; }
          },
        },
        calendar: () => ({ events: { list: mockList } }),
      },
    }));

    const { __resetDbForTests } = await import("@/lib/db/client");
    __resetDbForTests();
    const token = await import("@/lib/db/token-store");
    await token.upsertAccount({
      userId: 1, googleEmail: "a@example.com", refreshToken: "r1",
      accessToken: null, accessTokenExpiresAt: null,
    });
    await token.upsertAccount({
      userId: 1, googleEmail: "b@example.com", refreshToken: "r2",
      accessToken: null, accessTokenExpiresAt: null,
    });

    const { syncUserCalendars } = await import("@/lib/sync/sync-user");
    const result = await syncUserCalendars(1, {
      start: new Date("2026-04-06T00:00:00Z"),
      end: new Date("2026-04-20T00:00:00Z"),
    });
    expect(result.perAccount).toHaveLength(2);
    expect(result.perAccount.every((r) => r.status === "ok")).toBe(true);

    const ev = await import("@/lib/db/event-store");
    const events = await ev.listEventsInWindow(1, {
      start: new Date("2026-04-06T00:00:00Z"),
      end: new Date("2026-04-20T00:00:00Z"),
    });
    expect(events).toHaveLength(2);
  });

  it("surfaces per-account reauth_required without blocking other accounts", async () => {
    const mockList = vi.fn()
      .mockImplementationOnce(async () => { const e: any = new Error("invalid_grant"); e.response = { data: { error: "invalid_grant" } }; throw e; })
      .mockResolvedValueOnce({ data: { items: [
        { id: "b1", summary: "two", status: "confirmed",
          start: { dateTime: "2026-04-11T09:00:00Z" },
          end: { dateTime: "2026-04-11T10:00:00Z" } },
      ] } });

    vi.doMock("googleapis", () => ({
      google: {
        auth: {
          OAuth2: class {
            setCredentials() {}
            async getAccessToken() { return { token: "at" }; }
          },
        },
        calendar: () => ({ events: { list: mockList } }),
      },
    }));

    const { __resetDbForTests } = await import("@/lib/db/client");
    __resetDbForTests();
    const token = await import("@/lib/db/token-store");
    await token.upsertAccount({
      userId: 1, googleEmail: "a@example.com", refreshToken: "r1",
      accessToken: null, accessTokenExpiresAt: null,
    });
    await token.upsertAccount({
      userId: 1, googleEmail: "b@example.com", refreshToken: "r2",
      accessToken: null, accessTokenExpiresAt: null,
    });

    const { syncUserCalendars } = await import("@/lib/sync/sync-user");
    const result = await syncUserCalendars(1);
    const statuses = result.perAccount.map((r) => r.status).sort();
    expect(statuses).toEqual(["ok", "reauth_required"]);
  });
});
```

- [ ] **Step 2: Run — verify failure**

Run: `npm test -- sync-user`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `lib/sync/sync-user.ts`**

```ts
import { listAccountsForUser, getDecryptedRefreshToken } from "@/lib/db/token-store";
import { replaceWindow } from "@/lib/db/event-store";
import { GoogleCalendarClient, ReauthRequired, SyncFailed } from "@/lib/google/client";
import { mapGoogleEvent } from "@/lib/google/map";
import { logger } from "@/lib/logger";

export type PerAccountResult =
  | { accountId: number; googleEmail: string; status: "ok"; eventCount: number }
  | { accountId: number; googleEmail: string; status: "reauth_required" }
  | { accountId: number; googleEmail: string; status: "error"; reason: string };

export type SyncResult = {
  syncedAt: Date;
  perAccount: PerAccountResult[];
};

export function defaultWindow(now = new Date()): { start: Date; end: Date } {
  // Start of this week (Sunday 00:00 local) → end of next week (Sunday +14 days)
  const d = new Date(now);
  const dayOfWeek = d.getDay();
  d.setHours(0, 0, 0, 0);
  d.setDate(d.getDate() - dayOfWeek);
  const end = new Date(d);
  end.setDate(end.getDate() + 14);
  return { start: d, end };
}

export async function syncUserCalendars(
  userId: number,
  window?: { start: Date; end: Date }
): Promise<SyncResult> {
  const accounts = await listAccountsForUser(userId);
  const syncWindow = window ?? defaultWindow();

  const perAccount = await Promise.all(
    accounts.map(async (a): Promise<PerAccountResult> => {
      try {
        const refreshToken = await getDecryptedRefreshToken(a.id);
        const client = new GoogleCalendarClient({
          accountId: a.id,
          refreshToken,
          accessToken: a.accessToken ?? null,
          accessTokenExpiresAt: a.accessTokenExpiresAt ?? null,
        });
        const raw = await client.listEvents({
          timeMin: syncWindow.start,
          timeMax: syncWindow.end,
        });
        const mapped = raw.map(mapGoogleEvent);
        await replaceWindow(a.id, userId, syncWindow, mapped);
        return {
          accountId: a.id,
          googleEmail: a.googleEmail,
          status: "ok",
          eventCount: mapped.length,
        };
      } catch (err) {
        if (err instanceof ReauthRequired) {
          logger.warn({ accountId: a.id }, "reauth required");
          return { accountId: a.id, googleEmail: a.googleEmail, status: "reauth_required" };
        }
        logger.error({ accountId: a.id, err: String(err) }, "sync failed");
        return {
          accountId: a.id,
          googleEmail: a.googleEmail,
          status: "error",
          reason: err instanceof SyncFailed ? err.reason : String(err),
        };
      }
    })
  );

  return { syncedAt: new Date(), perAccount };
}
```

- [ ] **Step 4: Run tests**

Run: `npm test -- sync-user`
Expected: PASS (both tests)

- [ ] **Step 5: Commit**

```bash
git add lib/sync/sync-user.ts tests/sync/sync-user.test.ts
git commit -m "feat: syncUserCalendars orchestrator with per-account results

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 10: Auth.js config & OAuth callback

**Files:**
- Create: `lib/auth/config.ts`, `app/api/auth/[...nextauth]/route.ts`, `auth.ts`

- [ ] **Step 1: Create `lib/auth/config.ts`**

```ts
import NextAuth, { type NextAuthConfig } from "next-auth";
import Google from "next-auth/providers/google";
import { config } from "@/lib/config";
import { upsertAccount } from "@/lib/db/token-store";
import { logger } from "@/lib/logger";

export const authConfig: NextAuthConfig = {
  secret: config.nextAuthSecret,
  providers: [
    Google({
      clientId: config.googleClientId,
      clientSecret: config.googleClientSecret,
      authorization: {
        params: {
          scope: "openid email https://www.googleapis.com/auth/calendar.readonly",
          access_type: "offline",
          prompt: "consent",
        },
      },
    }),
  ],
  callbacks: {
    async signIn({ account, profile }) {
      if (!account || account.provider !== "google") return false;
      const email = (profile as { email?: string } | undefined)?.email;
      if (!email) {
        logger.error("google profile has no email");
        return false;
      }
      if (!account.refresh_token) {
        logger.error({ email }, "no refresh_token returned — ensure prompt=consent");
        return false;
      }
      try {
        await upsertAccount({
          userId: 1,
          googleEmail: email,
          refreshToken: account.refresh_token,
          accessToken: account.access_token ?? null,
          accessTokenExpiresAt: account.expires_at
            ? new Date(account.expires_at * 1000)
            : null,
        });
      } catch (err) {
        logger.error({ err: String(err) }, "failed to persist account");
        return false;
      }
      return "/settings?connected=1";
    },
  },
  pages: {
    error: "/settings?error=1",
  },
};

export const { handlers, signIn, signOut, auth } = NextAuth(authConfig);
```

- [ ] **Step 2: Create `auth.ts` (re-export)**

Create `auth.ts` at repo root:
```ts
export { handlers, signIn, signOut, auth } from "@/lib/auth/config";
```

- [ ] **Step 3: Create `app/api/auth/[...nextauth]/route.ts`**

```ts
import { handlers } from "@/lib/auth/config";
export const { GET, POST } = handlers;
```

- [ ] **Step 4: Verify TS compiles**

Run: `npx tsc --noEmit`
Expected: no errors in auth files.

- [ ] **Step 5: Commit**

```bash
git add lib/auth/ app/api/auth/ auth.ts
git commit -m "feat: Auth.js v5 Google OAuth with calendar.readonly scope

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 11: API routes + server action

**Files:**
- Create: `app/api/events/route.ts`, `app/api/sync/route.ts`, `app/actions/sync.ts`

- [ ] **Step 1: Create `app/api/events/route.ts`**

```ts
import { NextRequest, NextResponse } from "next/server";
import { listEventsInWindow } from "@/lib/db/event-store";
import { listAccountsForUser } from "@/lib/db/token-store";
import { logger } from "@/lib/logger";

export async function GET(req: NextRequest) {
  try {
    const { searchParams } = new URL(req.url);
    const from = searchParams.get("from");
    const to = searchParams.get("to");
    if (!from || !to) {
      return NextResponse.json({ error: "from and to required" }, { status: 400 });
    }
    const events = await listEventsInWindow(1, {
      start: new Date(from),
      end: new Date(to),
    });
    const accounts = await listAccountsForUser(1);
    const colorByAccount = Object.fromEntries(
      accounts.map((a) => [a.id, a.displayColor])
    );
    return NextResponse.json({
      events: events.map((e) => ({
        id: e.id,
        accountId: e.accountId,
        color: colorByAccount[e.accountId],
        title: e.title,
        description: e.description,
        location: e.location,
        start: e.startTs.toISOString(),
        end: e.endTs.toISOString(),
        isAllDay: e.isAllDay,
        htmlLink: e.htmlLink,
        hangoutLink: e.hangoutLink,
      })),
    });
  } catch (err) {
    logger.error({ err: String(err) }, "/api/events failed");
    return NextResponse.json({ error: "internal" }, { status: 500 });
  }
}
```

- [ ] **Step 2: Create `app/api/sync/route.ts`**

```ts
import { NextResponse } from "next/server";
import { syncUserCalendars } from "@/lib/sync/sync-user";
import { logger } from "@/lib/logger";

export async function POST() {
  try {
    const result = await syncUserCalendars(1);
    return NextResponse.json(result);
  } catch (err) {
    logger.error({ err: String(err) }, "/api/sync failed");
    return NextResponse.json({ error: "sync_failed" }, { status: 500 });
  }
}
```

- [ ] **Step 3: Create `app/actions/sync.ts`**

```ts
"use server";

import { revalidatePath } from "next/cache";
import { syncUserCalendars, SyncResult } from "@/lib/sync/sync-user";

export async function syncCalendarsAction(): Promise<SyncResult> {
  const result = await syncUserCalendars(1);
  revalidatePath("/week");
  return result;
}
```

- [ ] **Step 4: Commit**

```bash
git add app/api/events/ app/api/sync/ app/actions/
git commit -m "feat: /api/events, /api/sync, and syncCalendarsAction server action

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 12: Week page + time helpers

**Files:**
- Create: `app/week/page.tsx`, `lib/time/week.ts`

- [ ] **Step 1: Create `lib/time/week.ts`**

```ts
export function startOfWeek(d: Date): Date {
  const r = new Date(d);
  r.setHours(0, 0, 0, 0);
  r.setDate(r.getDate() - r.getDay());
  return r;
}

export function addDays(d: Date, n: number): Date {
  const r = new Date(d);
  r.setDate(r.getDate() + n);
  return r;
}

export function weekDays(start: Date): Date[] {
  return Array.from({ length: 7 }, (_, i) => addDays(start, i));
}

export function dayLabel(d: Date): string {
  return d.toLocaleDateString(undefined, { weekday: "short", day: "numeric" });
}
```

- [ ] **Step 2: Create `app/week/page.tsx`**

```tsx
import { listEventsInWindow } from "@/lib/db/event-store";
import { listAccountsForUser } from "@/lib/db/token-store";
import { redirect } from "next/navigation";
import { startOfWeek, addDays } from "@/lib/time/week";
import { WeekGrid } from "@/components/WeekGrid";
import { SyncButton } from "@/components/SyncButton";
import { AccountBadge } from "@/components/AccountBadge";
import { ReconnectBanner } from "@/components/ReconnectBanner";
import { WeekSwitcher } from "@/components/WeekSwitcher";

type SearchParams = Promise<{ w?: string }>;

export default async function WeekPage({ searchParams }: { searchParams: SearchParams }) {
  const params = await searchParams;
  const accounts = await listAccountsForUser(1);
  if (accounts.length === 0) redirect("/settings");

  const offset = params.w === "next" ? 1 : 0;
  const weekStart = addDays(startOfWeek(new Date()), offset * 7);
  const weekEnd = addDays(weekStart, 7);

  const events = await listEventsInWindow(1, { start: weekStart, end: weekEnd });
  const colorByAccount = Object.fromEntries(accounts.map((a) => [a.id, a.displayColor]));

  const uiEvents = events.map((e) => ({
    id: e.id,
    accountId: e.accountId,
    color: colorByAccount[e.accountId],
    title: e.title ?? "(no title)",
    description: e.description ?? "",
    location: e.location ?? "",
    start: e.startTs.toISOString(),
    end: e.endTs.toISOString(),
    isAllDay: e.isAllDay,
    htmlLink: e.htmlLink ?? null,
    hangoutLink: e.hangoutLink ?? null,
  }));

  return (
    <main className="max-w-[1400px] mx-auto p-6">
      <header className="flex items-center justify-between mb-6">
        <div className="flex items-center gap-3">
          <h1 className="text-2xl font-semibold">calsync</h1>
          <div className="flex gap-2">
            {accounts.map((a) => (
              <AccountBadge key={a.id} email={a.googleEmail} color={a.displayColor} />
            ))}
          </div>
        </div>
        <div className="flex items-center gap-3">
          <WeekSwitcher current={offset === 0 ? "this" : "next"} />
          <SyncButton />
        </div>
      </header>
      <ReconnectBanner />
      <WeekGrid weekStart={weekStart.toISOString()} events={uiEvents} />
    </main>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add app/week/ lib/time/
git commit -m "feat: week page with event loading and layout

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 13: Settings page

**Files:**
- Create: `app/settings/page.tsx`

- [ ] **Step 1: Create `app/settings/page.tsx`**

```tsx
import { listAccountsForUser } from "@/lib/db/token-store";
import { signIn } from "@/lib/auth/config";
import Link from "next/link";

export default async function SettingsPage() {
  const accounts = await listAccountsForUser(1);
  return (
    <main className="max-w-2xl mx-auto p-6">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-semibold">Settings</h1>
        <Link href="/week" className="text-sm text-blue-600 hover:underline">
          ← Week view
        </Link>
      </div>

      <section className="mb-8">
        <h2 className="text-lg font-medium mb-3">Connected Google Accounts</h2>
        {accounts.length === 0 && (
          <p className="text-slate-500 mb-4">No accounts connected yet.</p>
        )}
        <ul className="space-y-2 mb-4">
          {accounts.map((a) => (
            <li key={a.id} className="flex items-center gap-3 p-3 bg-white rounded border border-slate-200">
              <span
                className="inline-block w-3 h-3 rounded-full"
                style={{ backgroundColor: a.displayColor }}
              />
              <span className="font-mono text-sm">{a.googleEmail}</span>
            </li>
          ))}
        </ul>
        <form action={async () => {
          "use server";
          await signIn("google", { redirectTo: "/settings?connected=1" });
        }}>
          <button
            type="submit"
            className="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
          >
            Connect Google Account
          </button>
        </form>
        <p className="text-xs text-slate-500 mt-3">
          You'll be redirected to Google to authorize read-only calendar access.
          Repeat this step to connect a second account (sign out of the first in Google).
        </p>
      </section>
    </main>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add app/settings/
git commit -m "feat: settings page with connect flow

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 14: Week grid components

**Files:**
- Create: `components/WeekGrid.tsx`, `components/DayColumn.tsx`, `components/EventBlock.tsx`, `components/AllDayStrip.tsx`

- [ ] **Step 1: Create `components/WeekGrid.tsx`**

```tsx
"use client";

import { useState } from "react";
import { DayColumn } from "./DayColumn";
import { AllDayStrip } from "./AllDayStrip";
import { EventPopover } from "./EventPopover";

export type UIEvent = {
  id: number;
  accountId: number;
  color: string;
  title: string;
  description: string;
  location: string;
  start: string;
  end: string;
  isAllDay: boolean;
  htmlLink: string | null;
  hangoutLink: string | null;
};

const HOURS = Array.from({ length: 14 }, (_, i) => i + 7); // 7am–8pm
const DAY_LABELS = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];

export function WeekGrid({ weekStart, events }: { weekStart: string; events: UIEvent[] }) {
  const start = new Date(weekStart);
  const [selected, setSelected] = useState<UIEvent | null>(null);

  const days = Array.from({ length: 7 }, (_, i) => {
    const d = new Date(start);
    d.setDate(d.getDate() + i);
    return d;
  });

  const eventsByDay: UIEvent[][] = days.map((d) => {
    const dayStart = new Date(d);
    const dayEnd = new Date(d);
    dayEnd.setDate(dayEnd.getDate() + 1);
    return events
      .filter((e) => !e.isAllDay)
      .filter((e) => {
        const s = new Date(e.start);
        return s >= dayStart && s < dayEnd;
      });
  });

  const allDayByDay: UIEvent[][] = days.map((d) => {
    const dayStart = new Date(d);
    const dayEnd = new Date(d);
    dayEnd.setDate(dayEnd.getDate() + 1);
    return events
      .filter((e) => e.isAllDay)
      .filter((e) => {
        const s = new Date(e.start);
        return s >= dayStart && s < dayEnd;
      });
  });

  return (
    <div className="bg-white rounded-lg border border-slate-200 overflow-hidden">
      {/* Header row */}
      <div className="grid" style={{ gridTemplateColumns: "60px repeat(7, 1fr)" }}>
        <div className="border-b border-slate-200 p-2" />
        {days.map((d, i) => (
          <div
            key={i}
            className="border-b border-l border-slate-200 p-2 text-center text-sm font-medium"
          >
            <div className="text-slate-500 text-xs">{DAY_LABELS[i]}</div>
            <div className="text-slate-900">{d.getDate()}</div>
          </div>
        ))}
      </div>

      {/* All-day strip */}
      <AllDayStrip days={days} eventsByDay={allDayByDay} onSelect={setSelected} />

      {/* Time grid */}
      <div className="grid" style={{ gridTemplateColumns: "60px repeat(7, 1fr)" }}>
        <div className="relative">
          {HOURS.map((h) => (
            <div key={h} className="h-14 text-xs text-slate-400 pr-2 text-right pt-0.5">
              {h % 12 || 12}{h < 12 ? "am" : "pm"}
            </div>
          ))}
        </div>
        {days.map((d, i) => (
          <DayColumn
            key={i}
            day={d}
            hours={HOURS}
            events={eventsByDay[i]}
            onSelect={setSelected}
          />
        ))}
      </div>

      {selected && <EventPopover event={selected} onClose={() => setSelected(null)} />}
    </div>
  );
}
```

- [ ] **Step 2: Create `components/DayColumn.tsx`**

```tsx
"use client";

import { EventBlock } from "./EventBlock";
import type { UIEvent } from "./WeekGrid";

const HOUR_HEIGHT = 56; // matches h-14

export function DayColumn({
  day,
  hours,
  events,
  onSelect,
}: {
  day: Date;
  hours: number[];
  events: UIEvent[];
  onSelect: (e: UIEvent) => void;
}) {
  const dayStart = new Date(day);
  dayStart.setHours(hours[0], 0, 0, 0);

  return (
    <div className="relative border-l border-slate-200">
      {hours.map((h) => (
        <div key={h} className="border-b border-slate-100" style={{ height: HOUR_HEIGHT }} />
      ))}
      {events.map((e) => {
        const s = new Date(e.start);
        const en = new Date(e.end);
        const topMin = (s.getTime() - dayStart.getTime()) / 60000;
        const durMin = (en.getTime() - s.getTime()) / 60000;
        const top = (topMin / 60) * HOUR_HEIGHT;
        const height = Math.max((durMin / 60) * HOUR_HEIGHT - 2, 18);
        if (top < 0 || top > hours.length * HOUR_HEIGHT) return null;
        return (
          <EventBlock
            key={e.id}
            event={e}
            top={top}
            height={height}
            onClick={() => onSelect(e)}
          />
        );
      })}
    </div>
  );
}
```

- [ ] **Step 3: Create `components/EventBlock.tsx`**

```tsx
"use client";

import type { UIEvent } from "./WeekGrid";

export function EventBlock({
  event,
  top,
  height,
  onClick,
}: {
  event: UIEvent;
  top: number;
  height: number;
  onClick: () => void;
}) {
  return (
    <button
      onClick={onClick}
      className="absolute left-1 right-1 rounded px-1.5 py-0.5 text-xs text-white text-left truncate hover:brightness-110"
      style={{
        top,
        height,
        backgroundColor: event.color,
      }}
      title={event.title}
    >
      <div className="font-medium truncate">{event.title}</div>
      <div className="opacity-90 text-[10px] truncate">
        {new Date(event.start).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}
      </div>
    </button>
  );
}
```

- [ ] **Step 4: Create `components/AllDayStrip.tsx`**

```tsx
"use client";

import type { UIEvent } from "./WeekGrid";

export function AllDayStrip({
  days,
  eventsByDay,
  onSelect,
}: {
  days: Date[];
  eventsByDay: UIEvent[][];
  onSelect: (e: UIEvent) => void;
}) {
  const hasAny = eventsByDay.some((arr) => arr.length > 0);
  if (!hasAny) return null;
  return (
    <div
      className="grid border-b border-slate-200 bg-slate-50"
      style={{ gridTemplateColumns: "60px repeat(7, 1fr)" }}
    >
      <div className="text-xs text-slate-400 pr-2 text-right pt-1">all-day</div>
      {days.map((_, i) => (
        <div key={i} className="border-l border-slate-200 p-1 min-h-[32px] space-y-1">
          {eventsByDay[i].map((e) => (
            <button
              key={e.id}
              onClick={() => onSelect(e)}
              className="block w-full text-left text-xs truncate rounded px-1.5 py-0.5 text-white hover:brightness-110"
              style={{ backgroundColor: e.color }}
              title={e.title}
            >
              {e.title}
            </button>
          ))}
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 5: Commit**

```bash
git add components/WeekGrid.tsx components/DayColumn.tsx components/EventBlock.tsx components/AllDayStrip.tsx
git commit -m "feat: week grid with day columns, event blocks, all-day strip

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 15: SyncButton, EventPopover, AccountBadge, ReconnectBanner, WeekSwitcher

**Files:**
- Create: `components/SyncButton.tsx`, `components/EventPopover.tsx`, `components/AccountBadge.tsx`, `components/ReconnectBanner.tsx`, `components/WeekSwitcher.tsx`

- [ ] **Step 1: Create `components/SyncButton.tsx`**

```tsx
"use client";

import { useState, useTransition } from "react";
import { syncCalendarsAction } from "@/app/actions/sync";

export function SyncButton() {
  const [pending, startTransition] = useTransition();
  const [lastResult, setLastResult] = useState<string | null>(null);

  const onClick = () => {
    setLastResult(null);
    startTransition(async () => {
      try {
        const result = await syncCalendarsAction();
        const ok = result.perAccount.filter((r) => r.status === "ok").length;
        setLastResult(`Synced ${ok}/${result.perAccount.length} accounts`);
      } catch (err) {
        setLastResult(`Sync failed`);
      }
    });
  };

  return (
    <div className="flex items-center gap-3">
      {lastResult && <span className="text-xs text-slate-500">{lastResult}</span>}
      <button
        onClick={onClick}
        disabled={pending}
        className="relative px-4 py-2 rounded bg-slate-900 text-white text-sm font-medium disabled:opacity-80 overflow-hidden"
      >
        <span className={pending ? "opacity-60" : ""}>
          {pending ? "Syncing…" : "Sync"}
        </span>
        {pending && (
          <span
            className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent"
            style={{
              animation: "shimmer 1.2s linear infinite",
              backgroundSize: "200% 100%",
            }}
          />
        )}
      </button>
      <style>{`
        @keyframes shimmer {
          0% { background-position: 200% 0; }
          100% { background-position: -200% 0; }
        }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 2: Create `components/EventPopover.tsx`**

```tsx
"use client";

import { useEffect } from "react";
import type { UIEvent } from "./WeekGrid";

export function EventPopover({ event, onClose }: { event: UIEvent; onClose: () => void }) {
  useEffect(() => {
    const h = (e: KeyboardEvent) => { if (e.key === "Escape") onClose(); };
    window.addEventListener("keydown", h);
    return () => window.removeEventListener("keydown", h);
  }, [onClose]);

  const s = new Date(event.start);
  const en = new Date(event.end);
  const timeLabel = event.isAllDay
    ? `${s.toLocaleDateString()} (all day)`
    : `${s.toLocaleDateString()} · ${s.toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })} – ${en.toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}`;

  return (
    <div
      className="fixed inset-0 bg-black/30 flex items-center justify-center p-4 z-50"
      onClick={onClose}
    >
      <div
        className="bg-white rounded-lg shadow-xl max-w-md w-full p-5"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="flex items-start gap-2 mb-3">
          <span
            className="inline-block w-3 h-3 rounded-full mt-1.5"
            style={{ backgroundColor: event.color }}
          />
          <h2 className="text-lg font-semibold flex-1">{event.title}</h2>
        </div>
        <div className="text-sm text-slate-600 mb-2">{timeLabel}</div>
        {event.location && (
          <div className="text-sm text-slate-600 mb-2">📍 {event.location}</div>
        )}
        {event.description && (
          <div className="text-sm text-slate-700 mt-3 whitespace-pre-wrap max-h-40 overflow-y-auto">
            {event.description}
          </div>
        )}
        <div className="flex gap-2 mt-5">
          {event.hangoutLink && (
            <a
              href={event.hangoutLink}
              target="_blank"
              rel="noopener"
              className="px-3 py-1.5 bg-green-600 text-white text-sm rounded hover:bg-green-700"
            >
              Join meet
            </a>
          )}
          {event.htmlLink && (
            <a
              href={event.htmlLink}
              target="_blank"
              rel="noopener"
              className="px-3 py-1.5 bg-blue-600 text-white text-sm rounded hover:bg-blue-700"
            >
              Open in Google Calendar
            </a>
          )}
          <button
            onClick={onClose}
            className="px-3 py-1.5 border border-slate-300 rounded text-sm hover:bg-slate-50 ml-auto"
          >
            Close
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Create `components/AccountBadge.tsx`**

```tsx
export function AccountBadge({ email, color }: { email: string; color: string }) {
  return (
    <div className="flex items-center gap-1.5 px-2 py-1 bg-white rounded-full border border-slate-200 text-xs">
      <span
        className="inline-block w-2 h-2 rounded-full"
        style={{ backgroundColor: color }}
      />
      <span className="font-mono text-slate-700">{email}</span>
    </div>
  );
}
```

- [ ] **Step 4: Create `components/ReconnectBanner.tsx`**

```tsx
"use client";

import { useEffect, useState } from "react";
import Link from "next/link";

// Placeholder: in MVP this is shown when a previous sync returned reauth_required.
// For simplicity we read a flag from sessionStorage set by SyncButton on reauth result.
export function ReconnectBanner() {
  const [affected, setAffected] = useState<string[]>([]);
  useEffect(() => {
    try {
      const raw = sessionStorage.getItem("calsync:reauth");
      if (raw) setAffected(JSON.parse(raw));
    } catch {}
  }, []);
  if (affected.length === 0) return null;
  return (
    <div className="mb-4 p-3 rounded bg-amber-50 border border-amber-200 text-sm text-amber-900 flex items-center justify-between">
      <div>
        One or more Google accounts need to be reconnected:{" "}
        <span className="font-mono">{affected.join(", ")}</span>
      </div>
      <Link href="/settings" className="underline font-medium">Reconnect</Link>
    </div>
  );
}
```

- [ ] **Step 5: Create `components/WeekSwitcher.tsx`**

```tsx
import Link from "next/link";

export function WeekSwitcher({ current }: { current: "this" | "next" }) {
  return (
    <div className="inline-flex rounded border border-slate-200 overflow-hidden text-sm">
      <Link
        href="/week"
        className={`px-3 py-1.5 ${current === "this" ? "bg-slate-900 text-white" : "bg-white text-slate-700 hover:bg-slate-50"}`}
      >
        This week
      </Link>
      <Link
        href="/week?w=next"
        className={`px-3 py-1.5 border-l border-slate-200 ${current === "next" ? "bg-slate-900 text-white" : "bg-white text-slate-700 hover:bg-slate-50"}`}
      >
        Next week
      </Link>
    </div>
  );
}
```

- [ ] **Step 6: Update SyncButton to persist reauth list (edit Step 1's file)**

Edit `components/SyncButton.tsx`, replace the `onClick` body:
```tsx
  const onClick = () => {
    setLastResult(null);
    startTransition(async () => {
      try {
        const result = await syncCalendarsAction();
        const ok = result.perAccount.filter((r) => r.status === "ok").length;
        const reauth = result.perAccount
          .filter((r) => r.status === "reauth_required")
          .map((r) => r.googleEmail);
        try {
          if (reauth.length > 0) {
            sessionStorage.setItem("calsync:reauth", JSON.stringify(reauth));
          } else {
            sessionStorage.removeItem("calsync:reauth");
          }
        } catch {}
        setLastResult(`Synced ${ok}/${result.perAccount.length} accounts`);
      } catch (err) {
        setLastResult(`Sync failed`);
      }
    });
  };
```

- [ ] **Step 7: TS compile check**

Run: `npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 8: Commit**

```bash
git add components/SyncButton.tsx components/EventPopover.tsx components/AccountBadge.tsx components/ReconnectBanner.tsx components/WeekSwitcher.tsx
git commit -m "feat: sync button with shimmer + event popover + badges + week switcher

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 16: Seed script for UI development

**Files:**
- Create: `scripts/seed-test-data.ts`

- [ ] **Step 1: Create `scripts/seed-test-data.ts`**

```ts
import "dotenv/config";
import { __resetDbForTests } from "@/lib/db/client";
import { upsertAccount } from "@/lib/db/token-store";
import { replaceWindow } from "@/lib/db/event-store";
import { startOfWeek, addDays } from "@/lib/time/week";

async function main() {
  if (!process.env.ENCRYPTION_KEY) {
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 42).toString("base64");
  }
  if (!process.env.DATABASE_URL) {
    process.env.DATABASE_URL = "file:./calsync.db";
  }
  process.env.GOOGLE_CLIENT_ID ??= "stub";
  process.env.GOOGLE_CLIENT_SECRET ??= "stub";
  process.env.NEXTAUTH_SECRET ??= "stub";
  process.env.NEXTAUTH_URL ??= "http://localhost:3000";

  __resetDbForTests();

  const a = await upsertAccount({
    userId: 1, googleEmail: "primary@example.com", refreshToken: "stub-refresh-1",
    accessToken: null, accessTokenExpiresAt: null,
  });
  const b = await upsertAccount({
    userId: 1, googleEmail: "work@example.com", refreshToken: "stub-refresh-2",
    accessToken: null, accessTokenExpiresAt: null,
  });

  const weekStart = startOfWeek(new Date());
  const window = { start: weekStart, end: addDays(weekStart, 14) };

  const mkEvent = (
    googleEventId: string, title: string, day: number, hourStart: number, hourEnd: number
  ) => ({
    googleEventId, icalUid: null, title, description: "seeded", location: "Zoom",
    startTs: (() => { const d = addDays(weekStart, day); d.setHours(hourStart, 0, 0, 0); return d; })(),
    endTs: (() => { const d = addDays(weekStart, day); d.setHours(hourEnd, 0, 0, 0); return d; })(),
    isAllDay: false, tz: "UTC",
    status: "confirmed", responseStatus: "accepted",
    htmlLink: "https://calendar.google.com", hangoutLink: null,
    rawJson: "{}",
  });

  await replaceWindow(a.id, 1, window, [
    mkEvent("a1", "Standup", 1, 9, 10),
    mkEvent("a2", "Design sync", 2, 13, 14),
    mkEvent("a3", "1:1 Sarah", 3, 11, 12),
  ]);
  await replaceWindow(b.id, 1, window, [
    mkEvent("b1", "CSE 599R", 1, 11, 13),
    mkEvent("b2", "LING 573", 2, 15, 17),
    mkEvent("b3", "Office hours", 4, 10, 11),
  ]);

  console.log("Seeded 6 events across 2 accounts for week starting", weekStart.toISOString());
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 2: Run the seed script**

Run: `rm -f calsync.db && npm run seed`
Expected: `Seeded 6 events...`

- [ ] **Step 3: Boot dev server and smoke-test UI**

Run: `npm run dev` in background. Open `http://localhost:3000` in browser.
Expected: redirected to `/week`, seeded events appear in the grid in blue + green.

Stop the dev server.

- [ ] **Step 4: Commit**

```bash
git add scripts/seed-test-data.ts
git commit -m "feat: seed script for UI dev without OAuth round-trips

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Task 17: README with setup steps

**Files:**
- Create: `README.md`

- [ ] **Step 1: Create `README.md`**

```markdown
# calsync

Personal unified view of two Google Calendar accounts. Read-only, local-first, manual-sync.

## One-time setup

### 1. Google Cloud OAuth client

1. Go to https://console.cloud.google.com/ → new project
2. Enable **Google Calendar API**
3. APIs & Services → OAuth consent screen → External, fill minimums, add both your Gmail addresses as Test users
4. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID (type: Web application)
5. Add authorized redirect URI: `http://localhost:3000/api/auth/callback/google`
6. Copy Client ID + Client Secret

### 2. Environment variables

Copy `.env.local.example` to `.env.local` and fill in:

\`\`\`bash
cp .env.local.example .env.local

# Generate random secrets:
openssl rand -base64 32  # use for NEXTAUTH_SECRET
openssl rand -base64 32  # use for ENCRYPTION_KEY
\`\`\`

### 3. Install & run

\`\`\`bash
npm install
npm run db:generate    # create drizzle migration (first run only)
npm run dev
\`\`\`

Open http://localhost:3000 → you'll be redirected to `/settings` → connect both Google accounts.

## Usage

- `/week` — primary unified week grid view
- `/settings` — connect/reconnect Google accounts
- **Sync button** — pulls latest events from both accounts

## Development

\`\`\`bash
npm test               # run all Vitest unit/integration tests
npm run test:watch     # watch mode
npm run seed           # stub accounts + fake events for UI dev without OAuth
\`\`\`

## Architecture

See \`docs/superpowers/specs/2026-04-10-calsync-design.md\`.

Local phase uses SQLite; production will swap to Postgres via Drizzle by changing `DATABASE_URL`. The domain layer (`syncUserCalendars`) is pure and can be called from a cron without refactor.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: README with setup steps

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Self-Review

1. **Spec coverage**: every locked decision in the spec is implemented by a task. Auth.js OAuth → T10. Week grid + popover + shimmer → T14/T15. Replace-window sync semantics → T6 + T9. Reconnect path → T15 banner + T10 reconnect flow via settings.
2. **Placeholders**: none — every code block is complete, every command is specific.
3. **Type consistency**: `UIEvent`, `EventInput`, `Account`, `Event`, `SyncResult`, `PerAccountResult` all used consistently across tasks.
4. **Known shortcuts in MVP** (intentional, not placeholders):
   - `ReconnectBanner` reads from `sessionStorage` instead of server state — pragmatic short path, documented as future work.
   - No overlap handling for 3+ simultaneous events in a column — noted in spec future work.
   - `app/error.tsx` and `app/not-found.tsx` not explicitly tasked — Next.js provides default; add later if needed.
